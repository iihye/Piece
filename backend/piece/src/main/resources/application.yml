server:
  servlet:
    context-path: /api

spring:
  profiles:
    include: ${profile}

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace

    app:
      jwt:
        secret: "ssafy1234"
        expiration-ms: 3600000  # 1시간